{
  "name": "react-redux-autoconnect",
  "version": "1.0.5",
  "description": "Convention over configuration for react-redux's `connect()`",
  "main": "./lib/autoConnect.js",
  "author": "Robert Sargant",
  "license": "MIT",
  "repository": "https://github.com/sargant/react-redux-autoconnect",
  "scripts": {
    "compile": "babel src -d lib",
    "test": "jest",
    "prepublish": "npm run compile"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.5.0"
  },
  "peerDependencies": {
    "react-redux": ">=3.0.0"
  }
}
